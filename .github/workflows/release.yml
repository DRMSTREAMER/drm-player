name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-electron:
    name: Build Electron App
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: drm-player/package-lock.json

      - name: Install Dependencies
        working-directory: drm-player
        run: npm install

      - name: Build Windows
        if: matrix.os == 'windows-latest'
        working-directory: drm-player
        run: npm run build:win

      - name: Build macOS
        if: matrix.os == 'macos-latest'
        working-directory: drm-player
        run: npm run build:mac
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Build Linux
        if: matrix.os == 'ubuntu-latest'
        working-directory: drm-player
        run: npm run build:linux

      - name: Upload Windows Artifacts
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: drm-player/dist/*.exe

      - name: Upload macOS Artifacts
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: drm-player/dist/*.dmg

      - name: Upload Linux Artifacts
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: drm-player/dist/*.AppImage

  build-android:
    name: Build Android App
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      # 🔍 DEBUG – show repo structure
      - name: Debug Android directory
        run: |
          ls -la
          ls -la android-drm-player || true

      # ✅ FIX-1: make gradlew executable
      - name: Make gradlew executable
        working-directory: android-drm-player
        run: chmod +x gradlew

      - name: Build Debug APK
        working-directory: android-drm-player
        run: ./gradlew assembleDebug

      - name: Build Release APK
        working-directory: android-drm-player
        run: ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-build
          path: |
            android-drm-player/app/build/outputs/apk/debug/*.apk
            android-drm-player/app/build/outputs/apk/release/*.apk

  release:
    name: Create Release
    needs: [build-electron, build-android]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: artifacts/windows

      - uses: actions/download-artifact@v4
        with:
          name: macos-build
          path: artifacts/macos

      - uses: actions/download-artifact@v4
        with:
          name: linux-build
          path: artifacts/linux

      - uses: actions/download-artifact@v4
        with:
          name: android-build
          path: artifacts/android

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/windows/*
            artifacts/macos/*
            artifacts/linux/*
            artifacts/android/**/*.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
